let supervilains = require("supervillains")
let factorial = require("./funciones/funcion")
let supervillanos = supervillains.all

for (let i = 0; i < 4; i++) {
  
    console.log(supervillanos[factorial(math.floor(math.random() * (5-1) + 1))])
}